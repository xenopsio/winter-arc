# 1 September 2026 — Tuesday

## Done

* PCC — 30 min
* ESE — 25 min
* Natas — 15 min
* TryHackMe — **Linux Fundamentals (Part 1)** — 15 min

  * Very basic room; limited usefulness for current Linux skill level.
* Linux — counted from the TryHackMe session because the session was focused on Linux.

## Learned / Notes

### Python

* `.title()` capitalizes the first letter of each word.
* `.upper()` converts a string to uppercase.
* `.lower()` converts a string to lowercase.
* Python lists use square brackets `[]` and are separated by commas.
* Python uses **zero-based indexing**, meaning the first item is `[0]`.

### Linux

* `grep` can be used to search for text/content within files.
* `&` runs a process in the background.
* `&&` runs the next command only if the previous command succeeds.
* Learned how to write to a file without overwriting its existing contents.

### ESE

* Added `disconnect()` to close the SMTP session with `quit()`.
* Learned why using a `port` variable is better than hard-coding `587`.
* Learned the purpose of `starttls()` and `login()` in the SMTP connection process.

## Problems / Blockers

* Natas level was not solved.
* Linux VMs on Kali could not be accessed today.
* THM Premium rooms were unavailable, limiting the available rooms.

## Independent / Assisted

* **Independent:** PCC — solved/worked through independently.
* **Assisted:** ESE — used one external site to discover `disconnect()`.
* **Walkthrough:** None.

## Next

* [x] Natas — 25–30 min
* [x] Python PCC — 40–45 min
* [x] Programming/ESE — 45–60 min
* [x] Linux — 20–30 min
* [x] THM / HTB / foundations — 20–30 min

  * [TryHackMe Linux Fundamentals roadmap](https://github.com/uttambodara/TryHackMeRoadmap#linux-fundamentals)
