## Log

### 2 September 2026 (Wednesday) Arc

**Done**

- PCC 40–45min
- ESE 45–60min

**In Progress**

- 

**Learned / Notes**

- **PCC:**
    - Learned how to use variables inside strings with f-strings using `{}`.
    - Learned `.sort()` and how it sorts a list alphabetically.
    - Learned how `reverse=True` changes the sorting order.
    - Learned `sorted()` and the difference between it and `.sort()`. `sorted()` gives us a sorted version of the list without changing the original list.
    - Learned `\n` for creating a new line or line break inside a string.
- **ESE:**
    - Added the mail and app password to the configuration.
    - Ran the code after adding them. The code connected without errors, but no mail was sent because there was no message to send.
    - Added a message and assigned it to a variable.
    - Learned that simply assigning a message to a variable does not send it. We need `sendmail(sender, receiver, message)` to submit the email through the SMTP connection.
    - Looked at the current program flow to figure out where `sendmail()` should go.
    - Since the SMTP connection is already established inside `connect()` and the connection has not been closed yet, I placed `sendmail()` there.
    - Successfully added the first email-sending capability to the current ESE script.
    - Current flow: `connect()` → TLS → login → sendmail → `disconnect()`.

**Blockers**

- Appointments — remaining sessions could not be completed.

**Next**

- [ ]  Natas 25–30m
- [x]  Python PCC (Python Crash Course 3e) 40–45m
- [x]  Programming/ESE 45–60 min
- [ ]  Linux 20–30m
- [ ]  THM / HTB / foundations 20–30m https://github.com/uttambodara/TryHackMeRoadmap#linux-fundamentals

### Independent / Assisted

- [x]  Independent (Solved Myself) — PCC
- [x]  Assisted (Needed a hint or external information) — ESE
- [ ]  Walkthrough (Needed the actual solution)
